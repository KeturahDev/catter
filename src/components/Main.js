import React, { useEffect, useState } from 'react';

function Main (content) {
  // const [state, setState] = useState('default');
  // useEffect(() => {
  //   //fetch goes here
  // })

  // let defaultComponenet;
  // state !== 'default' ? setState(content) : null;

  // 
  return (
    <React.Fragment>
      <div style={{border: '2px solid green'}}>
        This is the main view where imma put stuff.
      </div>
    </React.Fragment>
  )
}

export default Main;
