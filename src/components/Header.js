import React from 'react';

function Header () {
    return (
      <React.Fragment>
        <header>Catter</header>
        <p>The app that nobody asked for, but aren't too upset about discovering.</p>
      </React.Fragment>
    )
}

export default Header;